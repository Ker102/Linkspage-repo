export interface WaveConfig {
  frequency: number;
  amplitude: number;
  speed: number;
  noiseStrength: number;
  colorStart: string;
  colorEnd: string;
}

export interface LineProps {
  index: number;
  count: number;
}